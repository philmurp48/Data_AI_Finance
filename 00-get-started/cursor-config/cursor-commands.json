{
  "$schema": "https://cursor.sh/schemas/commands.json",
  "name": "Accenture Project Commands",
  "description": "Scaffolding commands for creating brand-compliant Accenture prototypes",
  "version": "2.0.0",
  "commands": [
    {
      "name": "create-brand-page",
      "description": "Generate a new page with Accenture branding and layout",
      "template": "Create a new Next.js page with the following structure:\n\n1. **File Location:** `app/[page-name]/page.tsx`\n\n2. **Page Structure:**\n```tsx\nimport { BrandHeader, Section, PremiumCard } from '@/branding/ui';\nimport { Button } from '@/components/ui/button';\n\nexport default function [PageName]Page() {\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-slate-50 via-white to-purple-50/30\">\n      <BrandHeader \n        title=\"[Page Title]\"\n        subtitle=\"[Page Description]\"\n      />\n      \n      <main className=\"max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8\">\n        <Section \n          title=\"[Section Title]\"\n          description=\"[Section Description]\"\n        >\n          {/* Page content here */}\n          <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6\">\n            <PremiumCard>\n              <h3 className=\"text-lg font-semibold mb-2\">Card Title</h3>\n              <p className=\"text-gray-600\">Card content</p>\n            </PremiumCard>\n          </div>\n        </Section>\n      </main>\n    </div>\n  );\n}\n```\n\n3. **Required Features:**\n   - Gradient background using brand colors\n   - BrandHeader with Accenture branding\n   - Responsive layout (mobile-first)\n   - Semantic HTML structure\n   - Accessibility attributes\n\n4. **Styling:**\n   - Use Tailwind utility classes\n   - Follow spacing scale: 4, 6, 8\n   - Use brand purple accents (#A100FF)\n   - Responsive grid layouts\n\n5. **Customization:**\n   - Replace [PageName] with actual page name (PascalCase)\n   - Replace [Page Title] with display title\n   - Replace [Page Description] with subtitle\n   - Add specific content in section",
      "tags": ["page", "brand", "layout"]
    },
    {
      "name": "create-component",
      "description": "Generate a TypeScript component with proper structure",
      "template": "Create a new React component with the following structure:\n\n1. **File Location:** `components/[feature]/[component-name].tsx`\n\n2. **Component Structure:**\n```tsx\n/**\n * [ComponentName] - Brief description\n * \n * Detailed description of what this component does and when to use it.\n * \n * @example\n * <[ComponentName] prop1=\"value\" onAction={handler} />\n */\n\nimport React from 'react';\nimport { cn } from '@/lib/utils';\n\ninterface [ComponentName]Props {\n  // Required props first\n  prop1: string;\n  prop2: number;\n  // Optional props last\n  className?: string;\n  onAction?: () => void;\n  children?: React.ReactNode;\n}\n\nexport function [ComponentName]({\n  prop1,\n  prop2,\n  className,\n  onAction,\n  children\n}: [ComponentName]Props) {\n  return (\n    <div className={cn(\n      \"base-classes-here\",\n      className\n    )}>\n      {children}\n    </div>\n  );\n}\n```\n\n3. **Requirements:**\n   - TypeScript interface for props\n   - JSDoc comment with description and example\n   - Proper import organization\n   - className prop for extensibility\n   - Use cn() utility for class merging\n\n4. **Naming:**\n   - File: kebab-case (user-profile.tsx)\n   - Component: PascalCase (UserProfile)\n   - Props interface: PascalCase + Props (UserProfileProps)",
      "tags": ["component", "typescript"]
    },
    {
      "name": "create-navigation",
      "description": "Generate navigation component with Accenture branding",
      "template": "Create a navigation component based on the pattern from acn-o2c-prototype:\n\n1. **File Location:** `components/layout/top-nav.tsx`\n\n2. **Navigation Structure:**\n```tsx\n'use client';\n\nimport Link from 'next/link';\nimport { usePathname } from 'next/navigation';\nimport { useState } from 'react';\nimport { cn } from '@/lib/utils';\nimport { ChevronDown, User, LogOut } from 'lucide-react';\n\nconst navigation = [\n  { name: 'Dashboard', href: '/dashboard', icon: LayoutDashboard },\n  { name: 'Section 2', href: '/section-2', icon: Icon2 },\n  // Add more navigation items\n];\n\ninterface TopNavProps {\n  user: {\n    name?: string | null;\n    email?: string | null;\n    role?: string;\n  };\n}\n\nexport function TopNav({ user }: TopNavProps) {\n  const pathname = usePathname();\n  const [showUserMenu, setShowUserMenu] = useState(false);\n\n  return (\n    <nav className=\"bg-[#002244] shadow-lg sticky top-0 z-50\">\n      <div className=\"px-6\">\n        <div className=\"flex items-center h-16\">\n          {/* Logo */}\n          <div className=\"flex-shrink-0\">\n            <Link href=\"/\" className=\"flex items-center space-x-3\">\n              <div className=\"h-12 w-12 rounded-lg flex items-center justify-center\">\n                <img src=\"/logo.png\" alt=\"Logo\" className=\"h-full w-full object-contain\" />\n              </div>\n              <div>\n                <h1 className=\"text-white font-bold text-sm\">Application Name</h1>\n                <p className=\"text-xs text-white opacity-80\">Subtitle</p>\n              </div>\n            </Link>\n          </div>\n\n          {/* Main Navigation */}\n          <nav className=\"flex-1 flex items-center justify-center space-x-2\">\n            {navigation.map((item) => {\n              const isActive = pathname === item.href || pathname.startsWith(item.href + '/');\n              return (\n                <Link\n                  key={item.name}\n                  href={item.href}\n                  className={cn(\n                    \"flex items-center px-4 py-2 rounded-md text-sm font-semibold transition-all\",\n                    isActive\n                      ? \"bg-[#00A3E0] text-white shadow-md\"\n                      : \"text-gray-300 hover:bg-[#003366] hover:text-white\"\n                  )}\n                >\n                  <item.icon className=\"h-4 w-4 mr-1.5\" />\n                  {item.name}\n                </Link>\n              );\n            })}\n          </nav>\n\n          {/* User Menu */}\n          <div className=\"relative\">\n            <button\n              onClick={() => setShowUserMenu(!showUserMenu)}\n              className=\"flex items-center space-x-2 hover:bg-[#003366] rounded-lg px-3 py-2\"\n            >\n              <div className=\"h-8 w-8 bg-[#00A3E0] rounded-full flex items-center justify-center\">\n                <User className=\"h-4 w-4 text-white\" />\n              </div>\n              <span className=\"text-sm text-white\">{user.name}</span>\n            </button>\n          </div>\n        </div>\n      </div>\n    </nav>\n  );\n}\n```\n\n3. **Customize:**\n   - Add your navigation items to the array\n   - Update colors if using client branding\n   - Add dropdown menus if needed\n   - Implement mobile responsive menu",
      "tags": ["navigation", "layout", "brand"]
    },
    {
      "name": "create-data-table",
      "description": "Generate a data table component with sorting and pagination",
      "template": "Create a professional data table component:\n\n1. **File Location:** `components/[feature]/[table-name].tsx`\n\n2. **Table Structure:**\n```tsx\nimport { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';\nimport { useState } from 'react';\nimport { ChevronDown, ChevronUp } from 'lucide-react';\n\ninterface DataItem {\n  id: string;\n  name: string;\n  amount: number;\n  status: string;\n  date: Date;\n}\n\ninterface DataTableProps {\n  data: DataItem[];\n  onRowClick?: (item: DataItem) => void;\n}\n\nexport function DataTable({ data, onRowClick }: DataTableProps) {\n  const [sortField, setSortField] = useState<keyof DataItem>('name');\n  const [sortDirection, setSortDirection] = useState<'asc' | 'desc'>('asc');\n\n  const handleSort = (field: keyof DataItem) => {\n    if (sortField === field) {\n      setSortDirection(sortDirection === 'asc' ? 'desc' : 'asc');\n    } else {\n      setSortField(field);\n      setSortDirection('asc');\n    }\n  };\n\n  const sortedData = [...data].sort((a, b) => {\n    const aValue = a[sortField];\n    const bValue = b[sortField];\n    const modifier = sortDirection === 'asc' ? 1 : -1;\n    return aValue > bValue ? modifier : -modifier;\n  });\n\n  return (\n    <div className=\"bg-white rounded-xl border border-gray-200 shadow-md overflow-hidden\">\n      <Table>\n        <TableHeader>\n          <TableRow>\n            <TableHead \n              className=\"cursor-pointer hover:bg-gray-50\"\n              onClick={() => handleSort('name')}\n            >\n              <div className=\"flex items-center\">\n                Name\n                {sortField === 'name' && (\n                  sortDirection === 'asc' \n                    ? <ChevronUp className=\"ml-1 h-4 w-4\" />\n                    : <ChevronDown className=\"ml-1 h-4 w-4\" />\n                )}\n              </div>\n            </TableHead>\n            <TableHead className=\"text-right\">Amount</TableHead>\n            <TableHead>Status</TableHead>\n            <TableHead>Date</TableHead>\n          </TableRow>\n        </TableHeader>\n        <TableBody>\n          {sortedData.map((item) => (\n            <TableRow\n              key={item.id}\n              onClick={() => onRowClick?.(item)}\n              className=\"hover:bg-gray-50 cursor-pointer\"\n            >\n              <TableCell className=\"font-medium\">{item.name}</TableCell>\n              <TableCell className=\"text-right tabular-nums\">\n                ${item.amount.toLocaleString()}\n              </TableCell>\n              <TableCell>\n                <span className=\"inline-flex items-center px-2 py-1 rounded-full text-xs font-medium\">\n                  {item.status}\n                </span>\n              </TableCell>\n              <TableCell>{new Date(item.date).toLocaleDateString()}</TableCell>\n            </TableRow>\n          ))}\n        </TableBody>\n      </Table>\n    </div>\n  );\n}\n```\n\n3. **Features Included:**\n   - Sortable columns\n   - Hover states\n   - Click handling\n   - Responsive design\n   - Financial number formatting\n\n4. **Add as needed:**\n   - Pagination\n   - Search/filter\n   - Row selection\n   - Column visibility toggle",
      "tags": ["table", "data", "component"]
    },
    {
      "name": "create-kpi-card",
      "description": "Generate a KPI card component with trend indicators",
      "template": "Create a KPI card component:\n\n1. **File Location:** `components/ui/kpi-card.tsx`\n\n2. **Component:**\n```tsx\nimport { TrendingUp, TrendingDown, Minus } from 'lucide-react';\nimport { cn } from '@/lib/utils';\n\ninterface KPICardProps {\n  title: string;\n  value: string | number;\n  trend?: 'up' | 'down' | 'neutral';\n  trendValue?: string;\n  icon?: React.ReactNode;\n  className?: string;\n}\n\nexport function KPICard({\n  title,\n  value,\n  trend,\n  trendValue,\n  icon,\n  className\n}: KPICardProps) {\n  const getTrendIcon = () => {\n    if (trend === 'up') return <TrendingUp className=\"h-4 w-4\" />;\n    if (trend === 'down') return <TrendingDown className=\"h-4 w-4\" />;\n    if (trend === 'neutral') return <Minus className=\"h-4 w-4\" />;\n    return null;\n  };\n\n  const getTrendColor = () => {\n    if (trend === 'up') return 'text-green-600';\n    if (trend === 'down') return 'text-red-600';\n    return 'text-gray-600';\n  };\n\n  return (\n    <div className={cn(\n      \"bg-white rounded-xl border border-gray-200 shadow-md p-6\",\n      \"hover:shadow-xl transition-shadow cursor-pointer\",\n      className\n    )}>\n      <div className=\"flex items-center justify-between mb-4\">\n        {icon && (\n          <div className=\"h-10 w-10 rounded-lg bg-primary-100 flex items-center justify-center\">\n            {icon}\n          </div>\n        )}\n        {trendValue && (\n          <span className={cn(\n            \"text-sm font-medium flex items-center\",\n            getTrendColor()\n          )}>\n            {getTrendIcon()}\n            <span className=\"ml-1\">{trendValue}</span>\n          </span>\n        )}\n      </div>\n      <h3 className=\"text-2xl font-bold text-gray-900 mb-1 tabular-nums\">{value}</h3>\n      <p className=\"text-sm text-gray-600\">{title}</p>\n    </div>\n  );\n}\n```\n\n3. **Usage:**\n```tsx\n<KPICard\n  title=\"Total Revenue\"\n  value=\"$4.2M\"\n  trend=\"up\"\n  trendValue=\"+12.5%\"\n  icon={<DollarSign className=\"h-6 w-6 text-primary-600\" />}\n/>\n```",
      "tags": ["kpi", "card", "metrics"]
    },
    {
      "name": "apply-brand-colors",
      "description": "Update components to use Accenture brand colors",
      "template": "Update existing components to use brand colors:\n\n1. **Import Brand Tokens:**\n```tsx\nimport { BRAND_COLORS } from '@/branding/ui';\n```\n\n2. **Replace Generic Colors:**\n\n```tsx\n// ❌ Before\nclassName=\"bg-blue-500 text-white\"\nclassName=\"text-green-600\"\nclassName=\"border-red-500\"\n\n// ✅ After\nclassName=\"bg-primary-500 text-white\"  // Accenture purple #A100FF\nclassName=\"text-success\"                // Brand success green\nclassName=\"border-danger\"                // Brand danger red\n```\n\n3. **Update Chart Colors:**\n```tsx\nimport { CHART_COLORS } from '@/branding/ui';\n\n// Use in chart configuration\nconst chartConfig = {\n  colors: CHART_COLORS,  // [#A100FF, #3b82f6, #10b981, ...]\n};\n```\n\n4. **Status Colors:**\n```tsx\nconst getStatusColor = (status: string) => {\n  switch (status) {\n    case 'completed': return 'bg-green-100 text-green-800';\n    case 'pending': return 'bg-yellow-100 text-yellow-800';\n    case 'error': return 'bg-red-100 text-red-800';\n    default: return 'bg-gray-100 text-gray-800';\n  }\n};\n```\n\n5. **Review Checklist:**\n   - [ ] Primary actions use Accenture purple\n   - [ ] Success states use brand green\n   - [ ] Warning states use brand amber\n   - [ ] Error states use brand red\n   - [ ] Neutral elements use brand grays\n   - [ ] Charts use CHART_COLORS array",
      "tags": ["branding", "colors", "update"]
    }
  ],
  "variables": {
    "brandColor": "#A100FF",
    "brandColorDark": "#7500C0",
    "brandColorDarkest": "#460073",
    "brandName": "Accenture",
    "logoPath": "/logo.png",
    "fontSans": "Graphik, Inter, system-ui, sans-serif",
    "fontMono": "Courier, JetBrains Mono, monospace"
  }
}

